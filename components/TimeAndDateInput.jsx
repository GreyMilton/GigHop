import React from 'react';
import { Text, View} from 'react-native';

export default function TimeAndDateInput({ navigation }) {
  return (
    <View>
      <Text>TimeAndDateInput</Text>
    </View>
  );
}